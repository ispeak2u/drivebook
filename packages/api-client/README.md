# API Client

Shared client helpers for calling DriveBook service APIs.

## Current Scope

- Placeholder TypeScript package.
- Future home for typed API clients used by `apps/web` and `apps/admin`.

Do not connect external APIs or production services during Phase 1.
